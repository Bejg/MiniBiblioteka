version: '3.8'

services:
  db:
    image: mongo:latest
    container_name: minibiblioteka-db
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    restart: always

volumes:
  mongo-data:
